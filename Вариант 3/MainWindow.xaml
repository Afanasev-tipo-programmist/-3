<Window x:Class="Вариант_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Вариант_3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>

            </ColumnDefinition>
            <ColumnDefinition >


            </ColumnDefinition>
            <ColumnDefinition>

            </ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <!--Левая часть-->
        
            <DockPanel LastChildFill="True">
                <Label DockPanel.Dock="Top" Content="Регистрация пользователя" FontSize="18"/>
                <Label Content="ФИО" FontSize="18"/>
                <TextBox Margin="5"/>
            </DockPanel>
        
        
        <!--Середина-->
        <Button Grid.Column="1" Height="32" Content="Зарегестрироваться" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Margin="5" FontSize="18">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
        
        <!--Правая часть-->
        <DockPanel LastChildFill="True" Grid.Column="2">
            <Label DockPanel.Dock="Top" Content="" FontSize="18"/>
            <Label Content="Email" FontSize="18" DockPanel.Dock="Right"/>
            <TextBox Margin="5"/>
        </DockPanel>
    </Grid>
    
</Window>
